= Parksmap 애플리케이션
:navtitle: Parksmap 애플리케이션
include::vars.adoc[]
:experimental:

이 섹션에서는 ``parksmap``이라고도 알려진 ParksMap 애플리케이션의 웹 구성 요소를 배포합니다. 이 구성 요소는 OpenShift의 서비스 디스커버리(service discovery) 메커니즘을 사용하여 백엔드 Service를 검색하고 해당 데이터를 지도에 표시합니다.

[.bordershadow]
image::roadshow-app-architecture-parksmap-1.png[ParksMap 프런트엔드와 백엔드 서비스 디스커버리 아키텍처 다이어그램,link="self",window=_blank]

[#deploy_your_first_image]
== 실습: 첫 번째 컨테이너 이미지 배포하기

가장 간단한 방법부터 시작해 보겠습니다. 평범한 Docker 형식 이미지를 OpenShift에서 실행해 보는 것입니다.
이 작업은 매우 간단하며, OpenShift에서는 웹 콘솔에서 직접 수행할 수 있습니다.

**OCP Console** 탭에서 **{ocp4_starter_project}** Project의 **토폴로지** 화면으로 이동합니다.

왼쪽 상단의 **+** 아이콘을 클릭합니다. OpenShift에 애플리케이션을 배포할 수 있는 여러 옵션이 있는 메뉴가 표시됩니다. 배포하려는 이미지의 정보를 지정할 수 있는 대화 상자를 열려면 **컨테이너 이미지**를 클릭합니다.

[.bordershadow]
image::openshift-console-add-container.png[Container images 배포 옵션을 입력하는 대화 상자,link="self",window=_blank]

'외부 레지스트리의 이미지 이름' 아래에 **Image Name** 필드에 다음을 복사하여 붙여넣습니다:

[source,bash,role="copypaste",subs="attributes"]
----
{ocp4_starter_parksmap_image}
----

그러면 OpenShift가 지정된 컨테이너 레지스트리에 접근하여 이미지 세부 정보를 가져옵니다.

화면은 다음과 같은 모습이 됩니다:

[.bordershadow]
image::parksmap-image.png[이미지 이름 입력 후 표시되는 parksmap 이미지 세부 정보 화면,link="self",window=_blank]

**런타임 아이콘**에서 OpenShift 토폴록지 화면에 표시할 아이콘을 선택합니다. 이 프런트엔드는 Spring Boot로 만들어졌으므로 **spring-boot**를 선택합니다.

다음 값이 설정되어 있는지 확인합니다:

**애플리케이션 이름** :
[source,text,role=copypaste]
----
workshop
----

**이름** :
[source,text,role=copypaste]
----
parksmap
----

NOTE: **애플리케이션 이름**은 전체 애플리케이션의 이름이며, **이름** 필드는 개별 구성 요소에 부여된 이름을 나타냅니다. 이 경우 ``parksmap`` 구성 요소가 배포할 컨테이너입니다.

**리소스 유형** 섹션에서 **배포(Deployment)**가 선택되어 있는지 확인합니다.

NOTE: **리소스 유형**에 대해서는 이후 챕터에서 자세히 다룹니다.

**"고급 옵션"** 아래에 있는 **경로 생성(Create a route)** 옆의 체크박스를 **해제**합니다. 학습을 위해 이 워크숍의 나중 단계에서 애플리케이션용 **경로(Route)**를 직접 생성해 볼 것입니다.

페이지 하단의 '이름을 클릭하여 고급 옵션에 액세스합니다' 부분에서 **라벨(Labels)**를 클릭하고, 나중에 이 배포(Deployment)를 식별하는 데 사용할 수 있는 레이블을 추가합니다. 레이블은 웹 콘솔과 명령줄에서 구성 요소를 식별하고 필터링하는 데 도움이 됩니다.

레이블 3개를 추가합니다. 각 레이블에 대해 name=value 쌍을 입력한 후 다음 레이블을 입력하기 전에 kbd:[Return] 키(키보드에 따라 kbd:[Enter] 키)를 누릅니다. 첫 번째 레이블은 전체 애플리케이션의 이름입니다.

[source,text,role=copypaste]
----
app=workshop
----

다음은 이 배포(Deployment)의 이름입니다.

[source,text,role=copypaste]
----
component=parksmap
----

마지막으로, 이 구성 요소가 전체 애플리케이션에서 수행하는 역할입니다.

[source,text,role=copypaste]
----
role=frontend
----

[.bordershadow]
image::parksmap-image-options.png[Runtime Icon과 이름 및 레이블을 설정하는 이미지 배포 옵션 화면,width=600,link="self",window=_blank]

[.bordershadow]
image::parksmap-advanced-image-options.png[Create a route 체크 해제 등 고급 옵션과 레이블 입력 화면,width=600,link="self",window=_blank]

이제 파란색 **만들기(Create)** 버튼을 클릭합니다. **토폴로지** 페이지로 이동하며, ``workshop`` 애플리케이션 내 ``parksmap`` 배포에 대한 시각화를 확인할 수 있습니다.

[.bordershadow]
image::parksmap-dc-topology.png[Topology 화면에 표시된 workshop 애플리케이션의 parksmap 배포 아이콘,width=400,link="self",window=_blank]

OpenShift에서 컨테이너 이미지를 배포하는 데 필요한 단계는 이것이 전부입니다. 이렇게 별다른 설정 없이 바로 동작하는 이유는, 이 이미지가 다음과 같은 몇 가지 모범 사례를 따르고 있기 때문입니다:

* **EXPOSE로 포트를 정의합니다**: Dockerfile에 ``EXPOSE`` 명령으로 포트를 선언해 두면, OpenShift가 이 이미지를 읽어 어떤 포트로 요청을 받는지 자동으로 인식합니다. 그래서 포트 번호를 따로 알려줄 필요가 없습니다.
* **특정 사용자로 실행되도록 강제하지 않습니다**: OpenShift는 보안을 위해 이미지가 정한 사용자가 아니라, 클러스터가 임의로 배정한 사용자 번호로 컨테이너를 실행합니다. 이미지가 **root 사용자**나 특정 사용자 이름으로만 동작하도록 하드코딩되어 있으면 이 과정에서 권한 오류로 실패하지만, 어떤 사용자 번호로 실행되어도 동작하도록 만들어진 이미지는 문제없이 그대로 실행됩니다.
* **종료되지 않는 단일 CMD를 갖습니다**: Kubernetes는 컨테이너의 메인 프로세스(``CMD``/``ENTRYPOINT``로 지정한 프로세스)가 계속 실행되고 있어야 Pod가 정상적으로 실행 중이라고 판단합니다. 한 번 실행되고 끝나버리는 스크립트 대신, 웹 서버처럼 계속 떠 있는 단일 프로세스를 시작 시 실행해야 하는 이유입니다.

이러한 내용은 이 애플리케이션의 https://github.com/openshift-roadshow/parksmap-web/blob/master/Dockerfile[Dockerfile^]에서 확인할 수 있습니다.

NOTE: 애플리케이션을 대규모로 배포할 때는 적절한 레이블을 지정하는 것이 중요합니다. OpenShift는 Overview 페이지에서 구성 요소를 정의하고 그룹화하기 위해 **app** 레이블을 사용합니다. 명시적으로 지정하지 않으면 OpenShift가 기본값으로 이 레이블을 생성합니다.

[#containers_and_pods]
== 배경 지식: 컨테이너와 Pod

컨테이너와 **Pod**는 같은 것이 아닙니다. **컨테이너**는 애플리케이션과 그 실행에 필요한 모든 것(런타임, 라이브러리, 설정)을 하나로 묶은 패키지 그 자체입니다. **Pod**는 OpenShift(및 Kubernetes)가 이 컨테이너를 실제로 클러스터에서 실행하기 위해 사용하는 배포 단위로, 하나 이상의 컨테이너를 감싸서 같은 네트워크와 저장 공간을 공유하도록 묶어줍니다. 즉 컨테이너는 "무엇을 실행할지"에 대한 것이고, Pod는 "그것을 클러스터에서 어떻게 실행할지"에 대한 것입니다.

OpenShift(및 Kubernetes)에서 배포 가능한 가장 작은 단위는 **Pod**입니다. **Pod**는 함께 배포되고 동일한 호스트에서 실행되는 것이 보장되는 하나 이상의 https://opencontainers.org/[OCI(Open Container Initiative) 컨테이너^] 그룹입니다. OCI는 컨테이너 이미지와 런타임이 따라야 할 업계 표준 규격으로, Docker나 CRI-O 같은 서로 다른 도구로 만든 컨테이너라도 이 표준을 따르기만 하면 OpenShift에서 동일한 방식으로 실행할 수 있습니다.

OpenShift 공식 문서에 따르면 다음과 같습니다:

[quote]
__
각 **Pod**는 고유한 IP 주소를 가지므로 전체 포트 공간을 독점적으로 사용하며, Pod 내의
컨테이너들은 스토리지를 공유할 수 있습니다. **Pod**는 하나 이상의 레이블로 "태그"될
수 있으며, 이 레이블은 한 번의 작업으로 **Pod** 그룹을 선택하고 관리하는 데
사용됩니다.
__

**Pod**는 여러 개의 OCI 컨테이너를 포함할 수 있습니다. 일반적인 개념은 **Pod**가
"메인 프로세스"와 그 프로세스와 함께 실행하고자 하는 보조 서비스들을 포함하는 것입니다. **Pod**에 넣을 수 있는 컨테이너의 예로는 Apache HTTPD
서버, 로그 분석기, 업로드된 파일을 관리하는 파일 서비스 등이 있습니다.

NOTE: 방금 배포한 ``parksmap`` Pod는 컨테이너 1개만 포함하는 가장 단순한 형태입니다. Pod가 여러 컨테이너를 포함할 수 있다는 것은 이번 실습에서 직접 확인하지는 않지만, 앞으로 다른 애플리케이션을 다룰 때 마주칠 수 있는 일반적인 개념으로 알아두면 됩니다.

[#examining_the_pod]
== 실습: Pod 살펴보기

Topology 화면에서 ``parksmap`` 항목을 클릭하면 배포에 대한 정보가 표시됩니다. 기본적으로 **Resources** 탭이 표시될 수 있습니다. 이 경우 **Details** 탭을 클릭합니다.

[.bordershadow]
image::switchtoresources.png[Pod 상세 정보를 보기 위해 Details 탭으로 전환하는 화면,link="self",window=_blank]

해당 패널에서 여러분의 작업으로 생성된 하나의 **Pod**가 있는 것을 확인할 수 있습니다.

[.bordershadow]
image::parksmap-overview.png[Details 탭에 표시된 parksmap Pod 1개 현황,link="self",window=_blank]

NOTE: 이 화면에서 애플리케이션에 Health check를 추가하라고 제안하는 정보 상자를 볼 수 있습니다. 이는 나중에 처리할 것이므로, 지금은 오른쪽 상단의 X 아이콘을 클릭해서 이 정보 상자를 닫으면 됩니다.

웹 콘솔에서 **Workloads -> Pods**로 이동하면 여러분의 **Project** 내에 생성된 모든 **Pod** 목록도 확인할 수 있습니다.

[.bordershadow]
image::parksmap-podlist.png[Workloads > Pods 메뉴의 프로젝트 내 전체 Pod 목록,link="self",window=_blank]

이 **Pod**는 단일 컨테이너를 포함하며, 이는 간단한 Spring Boot/Java 애플리케이션인
``parksmap`` 애플리케이션입니다.

명령줄에서도 **Pod**를 확인할 수 있습니다:

[source,bash,role="copypaste"]
----
oc get pods
----

다음과 유사한 출력이 표시됩니다:

[.console-output]
[source]
----
NAME                        READY   STATUS    RESTARTS   AGE
parksmap-6444cdcd46-hp9st   1/1     Running   0          50s
----

위 출력은 현재 **Project**에 있는 모든 **Pod**를 나열하며, **Pod** 이름, 상태,
재시작 횟수, 가동 시간을 포함합니다.

**Pod**의 이름을 알고 나면 ``oc get`` 명령을
사용하여 해당 **Pod**에 대한 더 자세한 정보를 얻을 수 있습니다.
다음 구문을 사용하여 출력 형식을 **YAML**로 변경합니다:

NOTE: 위의 ``oc get pods`` 출력에서 올바른 **Pod** 이름을 사용해야 합니다.

[source]
----
oc get pod <pod-name> -o yaml
----

다음과 유사한 출력이 표시됩니다(이 워크숍의 지면 관계상 일부가
생략되었습니다):

[source,text]
----
apiVersion: v1
kind: Pod
metadata:
  annotations:
    k8s.v1.cni.cncf.io/network-status: |-
      [{
          "name": "",
          "interface": "eth0",
          "ips": [
              "10.131.0.93"
          ],
          "default": true,
          "dns": {}
      }]
    k8s.v1.cni.cncf.io/networks-status: |-
      [{
          "name": "",
          "interface": "eth0",
          "ips": [
              "10.131.0.93"
          ],
          "default": true,
          "dns": {}
      }]
    openshift.io/generated-by: OpenShiftWebConsole
    openshift.io/scc: restricted
  creationTimestamp: "2021-01-05T17:00:32Z"
  generateName: parksmap-65c4f8b676-
  labels:
    app: parksmap
    component: parksmap
    deployment: parksmap
    pod-template-hash: 65c4f8b676
    role: frontend
...............
----

웹 인터페이스의 **Pod** 세부 정보
페이지에서도 동일한 정보의 대부분을 확인할 수 있습니다. **Pod**의 이름을 클릭하면
세부 정보 페이지로 이동합니다. 또는 **Topology** 페이지에서 ``parksmap`` Deployment를 클릭하고 **Resources**를 선택한 후 **Pod** 이름을 클릭해도 이동할 수 있습니다.

[.bordershadow]
image::parksmap-dc-resources.png[parksmap Deployment의 Resources 탭에 표시된 Pod 목록,link="self",window=_blank]

여기에서 설정, 메트릭, 환경 변수, 로그, 이벤트를 확인할 수 있고, 실행 중인 Pod에 대한 터미널 셸도 얻을 수 있습니다.

[.bordershadow]
image::parksmap-pod.png[설정/메트릭/로그/터미널을 확인할 수 있는 Pod 세부 정보 페이지,link="self",window=_blank]

[.bordershadow]
image::parksmap-pod-events.png[Pod 세부 정보 페이지의 Events 탭에 표시된 이벤트 목록,link="self",window=_blank]

``parksmap`` 이미지를 실행하는 데 다소 시간이 걸릴 수 있습니다. 이미지를 실행하도록 요청받은 각
OpenShift 노드는, 로컬에 캐시되어 있지 않다면 이미지를 pull(다운로드)해야
합니다. 이미지 다운로드와 배포 상태는 **Pod** 세부 정보 페이지에서 확인하거나, 명령
줄에서 앞서 사용했던 ``oc get pods`` 명령을 통해 확인할 수 있습니다.

[#customizing_image_lifecycle_behavior]
== 배경 지식: 이미지 라이프사이클 동작 커스터마이징하기

OpenShift가 노드의 CRI(Container Runtime Interface) 런타임(Docker 데몬 또는 CRI-O)에 이미지를 실행하도록 요청할 때마다, 런타임은 실행할 올바른 버전의 이미지를 가지고 있는지 확인합니다.
가지고 있지 않다면 지정된 레지스트리에서 이미지를 pull합니다.

이 동작을 커스터마이징하는 방법은 여러 가지가 있으며, link:{ocp4_starter_openshift_docs}/html/images/managing-images[OpenShift 공식 문서^]에 정리되어 있습니다.
